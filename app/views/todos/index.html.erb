<%= render 'shared/header' %>
<div class="app-container">
  <div class="input-wrapper">
    <input type="text" class="input-wrapper__input" placeholder="What do you want to do?"/>
    <button class="button ml-4">Add todo</button>
  </div>
  <%= render partial: 'todos/list', locals: { todos: @todos } %>
</div>
